# Frontend Environment Variables (Vite)
# Base URL of the backend API server. Swap this with your production backend URL (e.g. https://api.yourdomain.com/api) when deploying.
VITE_API_URL=http://localhost:5000/api
